<%= form_for(album_group) do |f| %>
  <% if album_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album_group.errors.count, "error") %> prohibited this album_group from being saved:</h2>

      <ul>
      <% album_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
